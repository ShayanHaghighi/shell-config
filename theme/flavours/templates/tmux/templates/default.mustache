set -ogq @thm_bg "#{{ base00-hex }}"
set -ogq @thm_fg "#{{ base05-hex }}"

set -ogq @thm_rosewater "#{{ base06-hex }}"
set -ogq @thm_flamingo "#{{ base0F-hex }}"
set -ogq @thm_mauve "#{{ base0E-hex }}"
set -ogq @thm_red "#{{ base08-hex }}"
set -ogq @thm_peach "#{{ base09-hex }}"
set -ogq @thm_yellow "#{{ base0A-hex }}"
set -ogq @thm_green "#{{ base0B-hex }}"
set -ogq @thm_teal "#{{ base0C-hex }}"
set -ogq @thm_blue "#{{ base0D-hex }}"
set -ogq @thm_lavender "#{{ base07-hex }}"

set -ogq @thm_surface_2 "#{{ base04-hex }}"
set -ogq @thm_surface_1 "#{{ base03-hex }}"
set -ogq @thm_surface_0 "#{{ base02-hex }}"
set -ogq @thm_mantle "#{{ base01-hex }}"
